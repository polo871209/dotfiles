# Pi-agent boundary

Within `.pi/agent/`, edit only this subtree unless the request explicitly includes another repository path. Repository boundary rules are in `../../AGENTS.md`.

Before creating or editing agent-facing instructions in this subtree, read `skills/writing-for-agents/SKILL.md`.
