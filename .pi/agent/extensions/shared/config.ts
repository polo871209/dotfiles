// Cross-extension UI constants. Anything referenced by more than one extension
// lives here so there is a single source of truth (no per-file duplication).

/**
 * Horizontal padding, in columns, applied across the harness UI — the padded
 * conversation/editor (tui.ts) and full-width overlays (ask/) — so everything
 * shares one gutter and aligns.
 */
export const PAD = 5;
