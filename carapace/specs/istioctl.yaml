# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: istioctl
description: Istio control interface
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Cobra([istioctl])"]
