#:schema https://github.com/joshmedeski/sesh/raw/main/sesh.schema.json
[default_session]
startup_command = "tmux rename-window pi; tmux new-window; tmux new-window -d; p"
preview_command = "eza --all --git --icons --color=always {}"
