[default_session]
startup_command = "[ -f .venv/bin/activate ] && source .venv/bin/activate ; clear"
preview_command = "eza --all --git --icons --color=always {}"

[[session]]
name = "dotfiles "
path = "~/dotfiles"
